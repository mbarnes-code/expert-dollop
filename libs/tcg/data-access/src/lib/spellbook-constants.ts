/**
 * Constants for the Commander Spellbook application.
 */

/** Default sort order for search results */
export const DEFAULT_ORDER = 'auto';

/** Default sort field for search results */
export const DEFAULT_SORT = 'popularity';

/** Default vendor for pricing */
export const DEFAULT_VENDOR = 'cardkingdom';

/** Default ordering string for API queries */
export const DEFAULT_ORDERING = '-popularity,identity_count,card_count,-created';
